<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tongxinyiliao.kzdoctorapp" >
    //网络访问权限
    <uses-permission android:name="android.permission.INTERNET" />
    //SD卡读取权限
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    //SD卡写入权限
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".main.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //登录
        <activity android:name=".activity.login.LoginActivity" />
        //重置密码
        <activity android:name=".activity.login.ResetPwdActivity" />
        //注册
        <activity android:name=".activity.login.RegisterActivity" />
        //认证
        <activity android:name=".activity.login.AuthActivity" />
        //测试的
        <activity android:name=".activity.AAActivity" />
        <activity
            android:name=".activity.webview.WebViewActivity"
            android:label="@string/title_activity_web_view" >
        </activity>
    </application>

</manifest>
